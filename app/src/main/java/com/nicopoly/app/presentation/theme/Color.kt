package com.nicopoly.app.presentation.theme

import androidx.compose.ui.graphics.Color

/**
 * Nueva paleta de colores de Nicopoly.
 * Inspirada en tonos modernos y profesionales para una tienda de ropa femenina.
 * 
 * Colores seleccionados para ofrecer coherencia visual, contraste adecuado
 * y estética profesional con un toque moderno.
 */
object NicopolyColors {

    // Primary - Rosa suave (para elementos principales)
    val PrimaryLight = Color(0xFFE91E63)
    val OnPrimaryLight = Color(0xFFFFFFFF)
    val PrimaryContainerLight = Color(0xFFF4B7D2)
    val OnPrimaryContainerLight = Color(0xFF380025)

    // Secondary - Azul claro (para acentos secundarios)
    val SecondaryLight = Color(0xFF64B5F6)
    val OnSecondaryLight = Color(0xFFFFFFFF)
    val SecondaryContainerLight = Color(0xFFB3E5FC)
    val OnSecondaryContainerLight = Color(0xFF00579D)

    // Tertiary - Verde (para elementos de interés secundario)
    val TertiaryLight = Color(0xFF8BC34A)
    val OnTertiaryLight = Color(0xFFFFFFFF)
    val TertiaryContainerLight = Color(0xFFC8E6C9)
    val OnTertiaryContainerLight = Color(0xFF2E7D32)

    // Surface - Blanco (fondo de componentes)
    val SurfaceLight = Color(0xFFFFFFFF)
    val OnSurfaceLight = Color(0xFF1A1A1A)
    val SurfaceVariantLight = Color(0xFFF5F5F5)
    val OnSurfaceVariantLight = Color(0xFF424242)
    val SurfaceContainerLight = Color(0xFFEAEAEA)

    // Background - Blanco (fondo de pantalla)
    val BackgroundLight = Color(0xFFFFFFFF)
    val OnBackgroundLight = Color(0xFF1A1A1A)

    // Error - Rojo (se mantiene para errores)
    val ErrorLight = Color(0xFFD32F2F)
    val OnErrorLight = Color(0xFFFFFFFF)

    // Outline - Gris medio (para bordes y divisiones)
    val OutlineLight = Color(0xFF9E9E9E)
    val OutlineVariantLight = Color(0xFFBDBDBD)

    // Primary - Rosa suave (para texto/iconos visibles sobre fondo negro)
    val PrimaryDark = Color(0xFFE91E63)
    val OnPrimaryDark = Color(0xFFFFFFFF)
    val PrimaryContainerDark = Color(0xFF5F002A)
    val OnPrimaryContainerDark = Color(0xFFF4B7D2)

    // Secondary - Azul claro (para acentos secundarios)
    val SecondaryDark = Color(0xFF64B5F6)
    val OnSecondaryDark = Color(0xFFFFFFFF)
    val SecondaryContainerDark = Color(0xFF003A6E)
    val OnSecondaryContainerDark = Color(0xFFB3E5FC)

    // Tertiary - Verde (para elementos de interés secundario)
    val TertiaryDark = Color(0xFF8BC34A)
    val OnTertiaryDark = Color(0xFFFFFFFF)
    val TertiaryContainerDark = Color(0xFF1B5E20)
    val OnTertiaryContainerDark = Color(0xFFC8E6C9)

    // Surface - Negro (fondo de componentes en modo oscuro)
    val SurfaceDark = Color(0xFF1A1A1A)
    val OnSurfaceDark = Color(0xFFFFFFFF)
    val SurfaceVariantDark = Color(0xFF2D2D2D)
    val OnSurfaceVariantDark = Color(0xFFCCCCCC)
    val SurfaceContainerDark = Color(0xFF3C3C3C)

    // Background - Negro (fondo de pantalla en modo oscuro)
    val BackgroundDark = Color(0xFF1A1A1A)
    val OnBackgroundDark = Color(0xFFFFFFFF)
}